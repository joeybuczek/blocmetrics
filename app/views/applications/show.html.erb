<%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>

<div class="row bgcolor-border margin-row">
  <div class="col-md-8">

    <h1><%= @application.name %></h1>
    <br/>
    
    <table class="table">
      <thead>
        <tr>
          <td><strong>Event Type</strong></td>
          <td><strong>Number of Events</strong></td>
        </tr>
      </thead>
      <tbody>
        <% @events.each do |e| %>
          <tr>
            <td><%= e[0] %></td>
            <td><%= e[1].count %></td>
          </tr>
        <% end %>     
      </tbody>
    </table>
    
    <table class="table">
      <thead>
        <tr>
          <td><strong>Event Charts</strong></td>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= pie_chart @application.events.group(:name).count %></td>
        </tr>
        <tr>
          <td><%= column_chart @application.events.group(:name).count %></td>
        </tr>
      </tbody>
    </table>
    
          
 
  </div>
</div>